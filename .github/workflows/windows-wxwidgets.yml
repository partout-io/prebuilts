name: Windows wxWidgets

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  WXWIDGETS_VERSION: "3.3.2"
  WXWIDGETS_SOURCE_SHA256: "f6a56de6d8fb55317230fba4ef64f81a646ad6f8c439d2710d98750493a8a569"
  MSVC_RUNTIME_LIBRARY: MultiThreadedDLL

jobs:
  build:
    name: ${{ matrix.arch }}
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
          - arch: arm64

    steps:
      - name: Check out prebuilts
        uses: actions/checkout@v7

      - name: Build wxWidgets
        shell: pwsh
        run: scripts/build-wxwidgets.ps1 -Arch "${{ matrix.arch }}"

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: wxwidgets-windows-${{ matrix.arch }}
          path: artifacts/*
          if-no-files-found: error
